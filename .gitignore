chats/
.claude/
.idea/

# bench/ outputs: per-run results, kernel checkouts, scratch caches.
bench/results/
bench/work/

# `go build ./agent/...` from the repo root drops a binary here.
/agent/agent
# `go build ./pause/...` from the repo root drops binaries here.
/pause/pause

# macOS metadata files
.DS_Store